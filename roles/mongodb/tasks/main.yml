- name: Configuring the mongodb repo
  ansible.builtin.copy: 
    src: mongodb.repo
    dest: /etc/yum.repos.d/mongodb.repo

- name: Installing mongoDB
  ansible.builtin.package:
    name: mongodb-org
    state: present

- name: Updating the Mongodb config to be public  
  ansible.builtin.replace:
    path: /etc/mongod.conf
    regexp: '127.0.0.1'
    replace: '0.0.0.0'
      
- name: starting mongodb
  ansible.builtin.systemd:
    name: mongod
    state: started
    enabled: true    

- name: Downloading and extracting the Mongodb scehma
  ansible.builtin.unarchive:
    src: https://github.com/stans-robot-project/{{ COMPONENT }}/archive/main.zip
    dest: /tmp
    remote_src: yes  

- name: Injecting the mongodb scehma
  community.mongodb.mongodb_shell:
     file: "/tmp/{{ COMPONENT }}-manin/{{item}}.js"
     idempotent: yes 
  loop:
    - catalogue
    - users     

# - name: Injecting the mongodb users scehma
#   community.mongodb.mongodb_shell:
#      file: "/tmp/{{ COMPONENT }}-manin/users.js"
#      idempotent: yes          